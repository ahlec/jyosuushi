mutation CreateCounterCollection($name: String!) {
  createCounterCollection(name: $name) {
    collection {
      id
      name
      counterIds
      dateCreated
      dateLastUpdated
    }
    error
  }
}
