mutation SubmitBugReport(
  $message: String!
  $userAgent: String!
  $clientVersion: String!
) {
  result: submitBugReport(
    message: $message
    userAgent: $userAgent
    clientVersion: $clientVersion
  ) {
    success
  }
}
