mutation SubmitBugReport(
  $message: String!
  $userAgent: String!
  $clientVersion: String!
) {
  submitBugReport(
    message: $message
    userAgent: $userAgent
    clientVersion: $clientVersion
  ) {
    success
  }
}
