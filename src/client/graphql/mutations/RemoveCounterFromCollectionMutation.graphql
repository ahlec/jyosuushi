mutation RemoveCounterFromCollection(
  $counterId: String!
  $collectionId: String!
) {
  removeCounterFromCollection(
    counterId: $counterId
    collectionId: $collectionId
  ) {
    collectionId
    counterId
    result
  }
}
