mutation AddCounterToCollection($counterId: String!, $collectionId: String!) {
  addCounterToCollection(counterId: $counterId, collectionId: $collectionId) {
    collectionId
    counterId
    result
  }
}
